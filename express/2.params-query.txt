http://localhost:3000/api/product/10?currency=usd

/:id
const id = req.params.id; --- id

/product?currency=usd
const query = req.query.currency; --- query

Query Strings Used for:
filtering
searching
pagination
sorting

Route Groups - Each file contains related routes. Scalable (Easy to grow large APIs.)
app.use("/api/users", userRoutes);
app.use("/api/product", productRoutes);

Method	Purpose
GET --	Fetch data
POST --	Create data
PUT --	Update full data
PATCH -- Update partial data
DELETE -- Remove data